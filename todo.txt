- [x] find out why it cannot find the current active app on display 0, since currently it gets the current active app across all displays
- [x] according to my observation, maybe the first candidate of current function "get_active_app_info" in file flashback/workers/window_title.py is the current focused display and its app running inside. we need to extract the true display id associated with every foreground app.
- [x] find out if we can get the current active display id, so we only take screenshot at that display instead.
- [x] infer unknown app name using a previous historical dict, only update when there is app name detected on that display id. use that dict to infer app name only for display 0
- [x] add config to take screenshot only for display 0 or current focused app, or current focused display
- [x] make the web ui more responsive, mobile friendly
- [x] make each screenshot in webview mobile display at least 2 photos per row, since we are browsing on mobile phone, and make buttons small on top.
- [x] make desktop view to view full size of the screenshot in current page height and width, since it could be vertical.
- [ ] since we cannot take screenshot in those virtual displays, and dumpsys SurfaceFlinger --display-id shows display 0 only, we might want to disable the "focused" option, only leave "main" mode as the only option. (since you use scrcpy and swm to spawn the virtual display, maybe you should work with swm codebase instead, record history of your interactions and provide webui for searching it? or run scrcpy-server with root permission in termux and communicate with it?)
- [x] create a global cache in worker window title fetcher, if an app name is known using app id in the database, skip copy apk and use aapt to detect its name, just use the database fetched result.
- [x] check beforehand worker launches, if capture target display is focused or all, and capture backend is screencap (not scrcpy), then stop the entire  screenshot worker since it is invalid. maybe stop all workers would be more reasonable? only if screenshot backend is enabled?
